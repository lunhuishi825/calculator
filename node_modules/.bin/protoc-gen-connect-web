#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@bufbuild/protoc-gen-connect-web/bin/protoc-gen-connect-web" "$@"
else 
  exec node  "$basedir/../@bufbuild/protoc-gen-connect-web/bin/protoc-gen-connect-web" "$@"
fi
